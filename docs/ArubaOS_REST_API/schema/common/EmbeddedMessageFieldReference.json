{
    "$schema": "http://json-schema.org/draft-04/schema#", 
    "description": "Wraps the recursive reference to an embedded field inside a PB message.", 
    "title": "EmbeddedMessageFieldReference", 
    "type": "object", 
    "properties":
    {
        "uri":
        {
            "description": "The URI of the configuration entity", 
            "type": "string", 
            "readonly": true
        }, 
        "next_reference":
        {
            "description": "Holds the next field reference inside the embedded message.", 
            "type": "array", 
            "items":
            {
                "$ref":"../../schema/common/MessageFieldReference.json"
            }
        }
    }
}
