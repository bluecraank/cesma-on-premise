{
    "$schema": "http://json-schema.org/draft-04/schema#", 
    "description": "Custom DST Rule configuration data. - If configured day falls on a Sunday then the time changes at 2AM on the configured day. - Otherwise, the time changes at 2AM on the first Sunday after the configured day.", 
    "title": "CustomDstRule", 
    "type": "object", 
    "properties":
    {
        "uri":
        {
            "description": "The URI of the configuration entity", 
            "type": "string", 
            "readonly": true
        }, 
        "day_of_month":
        {
            "description": "The [Day] of month.", 
            "minimum": 1, 
            "maximum": 31, 
            "type": "integer", 
            "sql.not_null": true
        }, 
        "month":
        {
            "description": "The [Month].", 
            "$ref":"../../schema/common/Month.json", 
            "sql.not_null": true
        }
    }
}
